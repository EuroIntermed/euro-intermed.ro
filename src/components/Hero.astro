---
/**
 * Hero — badge with pulsing dot, staggered fadeUp headline + lead, the
 * primary / secondary / WhatsApp actions, and the animated HeroFlow visual.
 */
import { t } from '../i18n/utils'
import type { Locale } from '../i18n/ui'
import { waLink } from '../lib/config'
import Button from './Button.astro'
import HeroFlow from './HeroFlow.astro'

const { locale } = Astro.props as { locale: Locale }
const c = t(locale).hero
---

<section class="ei-hero" aria-labelledby="ei-hero-title">
  <div class="ei-hero__bg" aria-hidden="true"></div>
  <div class="ei-container ei-hero__grid">
    <div class="ei-hero__content">
      <p class="ei-badge" data-hero style="--i:0">
        <span class="ei-badge__dot" aria-hidden="true"></span>
        {c.badge}
      </p>
      <h1 class="ei-hero__title" id="ei-hero-title" data-hero style="--i:1">{c.title}</h1>
      <p class="ei-lead ei-hero__lead" data-hero style="--i:2">{c.lead}</p>
      <div class="ei-hero__actions" data-hero style="--i:3">
        <Button href={waLink(c.whatsappText)} variant="whatsapp" target="_blank" rel="noopener" data-ga-vertical="whatsapp">
          {c.whatsapp}
        </Button>
        <Button href="#ecosystem" variant="secondary">{c.secondary}</Button>
      </div>
    </div>

    <div class="ei-hero__visual" data-hero style="--i:2">
      <HeroFlow locale={locale} />
    </div>
  </div>
</section>

<style>
  .ei-hero {
    position: relative;
    padding-block: clamp(3rem, 2rem + 6vw, 6.5rem) clamp(3.5rem, 2.5rem + 6vw, 7rem);
    overflow: hidden;
  }
  .ei-hero__bg {
    position: absolute;
    inset: -10% -20% auto -20%;
    height: 70%;
    background:
      radial-gradient(60% 80% at 78% 30%, var(--ei-accent-soft), transparent 70%),
      radial-gradient(50% 60% at 20% 10%, var(--ei-gold-soft), transparent 70%);
    pointer-events: none;
  }
  .ei-hero__grid {
    position: relative;
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    align-items: center;
    gap: clamp(2rem, 1rem + 4vw, 4.5rem);
  }
  .ei-hero__content {
    max-width: 38rem;
  }
  .ei-hero__title {
    margin-top: 1.1rem;
  }
  .ei-hero__lead {
    margin-top: 1.3rem;
  }
  .ei-hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 2rem;
  }
  .ei-hero__visual {
    display: flex;
    justify-content: center;
  }

  /* Hero entrance reveal (fires immediately, not scroll-gated) */
  [data-hero] {
    opacity: 0;
    transform: translateY(24px);
    animation: ei-fade-up 0.8s var(--ei-ease-out) forwards;
    animation-delay: calc(var(--i, 0) * 120ms + 100ms);
  }

  /* Badge with pulsing dot */
  .ei-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.6em;
    margin: 0;
    padding: 0.45em 0.95em;
    border: 1px solid var(--ei-hairline-strong);
    border-radius: var(--ei-radius-pill);
    background: var(--ei-surface);
    box-shadow: var(--ei-shadow-sm);
    font-size: 0.76rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--ei-ink-soft);
  }
  .ei-badge__dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--ei-accent);
    box-shadow: 0 0 0 4px var(--ei-accent-soft);
    animation: ei-badge-dot 2.4s ease-in-out infinite;
  }

  @media (max-width: 880px) {
    .ei-hero__grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .ei-hero__visual {
      order: -1;
    }
    .ei-hero__content {
      max-width: 100%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    [data-hero] {
      opacity: 1;
      transform: none;
    }
  }
</style>
